# راهنمای کاربر: ابزارهای تبدیل و ترکیب PDF/متن به Markdown ساخت‌یافته

## مقدمه

این پروژه شامل سه ابزار خط فرمان است که برای تبدیل اسناد PDF (به‌ویژه استانداردهای فنی مثل اسناد ایکائو) و همچنین پروژه‌های متنی/سورس‌کد به Markdown ساخت‌یافته و مناسب تحلیل توسط هوش مصنوعی طراحی شده‌اند:

1. **`pdf_to_markdown.py`** — یک یا چند فایل PDF (تک فایل، چند فایل، یک پوشه یا چند پوشه) را به فایل‌های Markdown ساخت‌یافته تبدیل می‌کند و در صورت نیاز خروجی تجمیعی نیز می‌سازد.
2. **`md_combiner.py`** — چند فایل Markdown (مثلاً خروجی‌های قبلی `pdf_to_markdown.py`) را در یک فایل واحد ترکیب می‌کند، با شناسه‌های یکتا و یکدست برای Anchorها و متادیتای JSON برای هر سند.
3. **`text_to_markdown.py`** — فایل‌های متنی/سورس‌کد یک یا چند پروژه (هر فایل غیرباینری) را در یک فایل Markdown واحد با همان قالب جداکننده‌ها ترکیب می‌کند؛ مناسب برای آپلود یک پروژه چندفایلی به ابزارهای هوش مصنوعی.

هر سه ابزار از یک قالب جداکننده‌ی مشترک استفاده می‌کنند:

```
<<<FILE_START:{...متادیتای JSON...}>>>
...محتوا...
<<<FILE_END:{...متادیتای JSON...}>>>
```

این قالب به ابزارهای هوش مصنوعی اجازه می‌دهد مرز هر فایل/سند را به‌دقت تشخیص دهند و محتوای اسناد/فایل‌های مختلف را با یکدیگر قاطی نکنند.

---

## ویژگی‌ها

### `pdf_to_markdown.py`
- **بندهای شماره‌دار:** شماره‌گذاری‌های فارسی/عربی و لاتین (مثل `5.4.2` یا `۴٫۳٫۲٫۱`) به هدینگ Markdown همراه با Anchor یکتا (مثل `{#id1-4.3.2.1}`) تبدیل می‌شوند.
- **تشخیص دو ستونه:** صفحاتی با چیدمان دو ستونی به‌درستی تفکیک و به ترتیب صحیح خوانده می‌شوند.
- **استخراج جدول:** جداول PDF به جدول استاندارد Markdown تبدیل می‌شوند؛ سلول‌های چندخطی با `<br>` نگه داشته می‌شوند.
- **حذف هدر/فوتر و مهر/امضا:** خطوط تکراری در بالا/پایین صفحات (هدر، فوتر، شماره صفحه، مهرهای رسمی) به‌صورت خودکار تشخیص و حذف می‌شوند.
- **پاک‌سازی artifact های `(cid:xx)`:** کدهای فونت معیوب که در استخراج متن PDF ظاهر می‌شوند، حذف می‌شوند.
- **فهرست مطالب (TOC):** در صورت وجود در PDF، به‌صورت هدینگ‌های Markdown به ابتدای فایل اضافه می‌شود.
- **ارجاع صفحه:** هر پاراگراف یا جدول با `[Page: X]` مشخص می‌شود.
- **ورودی‌های متنوع:** تک فایل PDF، چند فایل PDF، یک پوشه، یا چند پوشه (با جستجوی بازگشتی اختیاری).
- **خروجی تفکیکی و تجمیعی:** فایل جداگانه برای هر PDF، و در صورت درخواست، یک فایل تجمیعی با Anchorهای یکتا.

### `md_combiner.py`
- ترکیب چند فایل Markdown در یک فایل واحد با جداکننده‌های `<<<FILE_START/END>>>` و متادیتای JSON (شامل `id`، `name`، `pages`، `sha1`، `path`).
- یکدست‌سازی همه‌ی Anchorهای هر فایل به فرم `{#id<N>-...}` با اعداد یکتا و پیوسته، تا دو سند مختلف هرگز Anchor یکسان نداشته باشند.
- پشتیبانی از `--id-start` برای ادامه‌ی شماره‌گذاری در اجراهای بعدی (دسته‌های جدید اسناد).

### `text_to_markdown.py`
- تبدیل فایل‌های متنی/سورس‌کد یک یا چند پروژه به یک Markdown واحد با همان جداکننده‌های `<<<FILE_START/END>>>`.
- تشخیص خودکار فایل متنی (بر اساس نبود بایت null و قابل‌دیکود بودن UTF-8).
- تشخیص زبان برای Syntax Highlighting بر اساس پسوند فایل (پایتون، جاوااسکریپت، تایپ‌اسکریپت، Go، Rust، و غیره).
- نادیده‌گرفتن پوشه‌های وابستگی/بیلد (`.git`، `node_modules`، `venv`، `dist`، `build`، ...) و فایل‌های حساس/باینری (`.env`، `*.pem`، `*.key`، `id_rsa*`، قفل‌فایل‌ها، تصاویر، آرشیوها و ...) به‌صورت پیش‌فرض.
- انتخاب خودکار تعداد بک‌تیک (```` یا بیشتر) برای هر فایل تا با بلوک‌های کد تودرتوی داخل خود فایل تداخل نداشته باشد.

---

## پیش‌نیازها

- **سیستم‌عامل:** ویندوز، لینوکس یا مک
- **پایتون:** 3.8 یا بالاتر
- **کتابخانه‌ها (برای `pdf_to_markdown.py`):**
  ```bash
  pip install PyMuPDF pdfplumber tqdm
  ```

ابزارهای `md_combiner.py` و `text_to_markdown.py` فقط از کتابخانه‌های استاندارد پایتون (`argparse`، `hashlib`، `json`، `re`، `fnmatch`، `pathlib`) استفاده می‌کنند و نیازی به نصب چیز اضافه‌ای ندارند.

---

## نصب

1. مخزن را کلون کنید:
   ```bash
   git clone https://github.com/SaeedGhazi/ICAO_pdf_to_markdown.git
   cd ICAO_pdf_to_markdown
   ```
2. کتابخانه‌ها را نصب کنید:
   ```bash
   pip install -r requirements.txt
   ```

---

## استفاده

### ۱. `pdf_to_markdown.py` — تبدیل PDF به Markdown

```bash
python3 pdf_to_markdown.py [paths ...] [options]
```

`paths` می‌تواند هر ترکیبی از موارد زیر باشد (اگر چیزی داده نشود، پوشه‌ی جاری در نظر گرفته می‌شود):
- مسیر یک فایل PDF
- مسیر چند فایل PDF
- مسیر یک پوشه (همه‌ی PDFهای داخل آن پردازش می‌شوند)
- مسیر چند پوشه

| گزینه | توضیح |
|---|---|
| `paths` | فایل(های) PDF و/یا پوشه(های) ورودی (پیش‌فرض: `.`) |
| `-R`, `--recursive` | جستجوی بازگشتی برای PDF در زیرپوشه‌ها |
| `--pattern PATTERN` | الگوی جستجوی فایل هنگام دادن پوشه (پیش‌فرض: `*.pdf`) |
| `-o`, `--output-dir DIR` | پوشه خروجی برای فایل‌های Markdown (پیش‌فرض: `markdown_output`) |
| `--id-start N` | شماره شروع شناسه‌های `id` (برای یکتا ماندن Anchorها بین چند اجرا، پیش‌فرض: `1`) |
| `--combine [OUTPUT]` | علاوه بر خروجی تفکیکی، یک فایل Markdown تجمیعی هم بسازد (نام پیش‌فرض: `combined_output_with_separators.md`) |

**مثال‌ها:**

تبدیل همه‌ی PDFهای پوشه‌ی جاری (رفتار پیش‌فرض، مثل قبل):
```bash
python3 pdf_to_markdown.py
```

تبدیل یک فایل مشخص:
```bash
python3 pdf_to_markdown.py docs/icao_doc1.pdf
```

تبدیل چند فایل مشخص:
```bash
python3 pdf_to_markdown.py docs/icao_doc1.pdf docs/icao_doc2.pdf
```

تبدیل همه‌ی PDFهای یک پوشه (و زیرپوشه‌هایش) و ساخت یک خروجی تجمیعی:
```bash
python3 pdf_to_markdown.py pdfs/ -R --combine
```

تبدیل چند پوشه با هم، با پوشه‌ی خروجی دلخواه:
```bash
python3 pdf_to_markdown.py annex1/ annex2/ -R -o markdown_output --combine annexes_combined.md
```

ادامه‌ی شماره‌گذاری Anchorها در یک دسته‌ی جدید (مثلاً دسته‌ی قبلی تا `id10` رفته بود):
```bash
python3 pdf_to_markdown.py more_pdfs/ -R --id-start 11 --combine
```

📂 خروجی در پوشه‌ی مشخص‌شده (پیش‌فرض `markdown_output/`) ذخیره می‌شود: یک فایل `.md` به ازای هر PDF (با نام `<نام‌فایل>.md`؛ در صورت تکراری بودن نام، فایل دوم به‌صورت `id<N>_<نام‌فایل>.md` ذخیره می‌شود تا رونویسی نشود) و در صورت استفاده از `--combine`، یک فایل تجمیعی اضافی.

---

### ۲. `md_combiner.py` — ترکیب فایل‌های Markdown

```bash
python3 md_combiner.py [path] [options]
```

| گزینه | توضیح |
|---|---|
| `path` | پوشه‌ی ورودی (پیش‌فرض: پوشه‌ی جاری) |
| `-o`, `--output FILE` | نام فایل خروجی (پیش‌فرض: `combined_output_with_separators.md`) |
| `--pattern PATTERN` | الگوی جستجو (پیش‌فرض: `*.md`) |
| `-R`, `--recursive` | جستجوی بازگشتی در زیرپوشه‌ها |
| `--id-start N` | شماره شروع `id` (مثلاً `101` برای دسته‌ی دوم اسناد، پیش‌فرض: `1`) |

**مثال:**

```bash
python3 md_combiner.py markdown_output/ -o combined_output_with_separators.md
```

ترکیب یک دسته‌ی جدید اسناد به‌طوری‌که Anchorهایش با دسته‌ی قبلی (که تا `id10` رفته بود) برخورد نکند:
```bash
python3 md_combiner.py more_markdown/ -o combined_part2.md --id-start 11
```

برنامه در پایان مقدار `LAST_ID_NUMBER` را چاپ می‌کند تا بدانید برای دسته‌ی بعدی از کدام شماره ادامه دهید.

---

### ۳. `text_to_markdown.py` — ترکیب پروژه‌های متنی/سورس‌کد

```bash
python3 text_to_markdown.py [paths ...] [options]
```

`paths` می‌تواند فایل و/یا پوشه باشد (پیش‌فرض: پوشه‌ی جاری).

| گزینه | توضیح |
|---|---|
| `paths` | فایل(ها) و/یا پوشه(های) ورودی (پیش‌فرض: `.`) |
| `--no-recursive` | عدم جستجوی بازگشتی در زیرپوشه‌ها (پیش‌فرض: بازگشتی) |
| `-o`, `--output FILE` | نام فایل خروجی (پیش‌فرض: `code_combined.md`) |
| `--max-size BYTES` | حداکثر حجم هر فایل بر حسب بایت (پیش‌فرض: `1000000`) |
| `--exclude [PATTERN ...]` | نام پوشه یا الگوی فایل اضافی برای نادیده گرفتن (مثلاً `tests` یا `'*.spec.js'`) |
| `--title TITLE` | عنوان فایل خروجی (پیش‌فرض: `Project Source Combined`) |

**مثال‌ها:**

ترکیب همه‌ی فایل‌های متنی پروژه‌ی جاری:
```bash
python3 text_to_markdown.py . -o project_combined.md --title "My Project"
```

ترکیب چند پروژه با هم:
```bash
python3 text_to_markdown.py backend/ frontend/ -o full_stack_combined.md
```

نادیده‌گرفتن یک پوشه و یک الگوی اضافی:
```bash
python3 text_to_markdown.py . --exclude tests "*.spec.js" -o code_combined.md
```

📂 خروجی یک فایل Markdown واحد است: لیست فایل‌ها در ابتدا، و سپس هر فایل در یک بلوک کد جداگانه با جداکننده‌های `<<<FILE_START/END>>>` و متادیتای JSON (`index`، `path`، `size`، `sha1`).

> ℹ️ فایل‌های حساس (`.env`, `*.pem`, `*.key`, `id_rsa*` و ...) و پوشه‌های وابستگی/بیلد (`.git`, `node_modules`, `venv`, `dist`, `build`, ...) به‌صورت پیش‌فرض از خروجی حذف می‌شوند.

---

## ساختار خروجی

### مثال بخش از یک فایل PDF استخراج‌شده:
```markdown
#### ۴٫۳٫۲٫۱ {#id1-4.3.2.1}
The operator shall ensure that all systems are operational. [Page: 5]
```

### مثال جدول:
```markdown
| Code | Description     | Value |
|------|-----------------|-------|
| A1   | System Check    | 100   |
| A2   | Compliance Test | 200   |
[Page: 4]
```

### مثال یک ورودی در فایل تجمیعی PDFها (`md_combiner.py` یا `--combine`):
```markdown
<<<FILE_START:{"index": 1, "id": "id1", "name": "icao_doc1.md", "pages": 10, "sha1": "...", "path": "markdown_output/icao_doc1.md"}>>>
## 1. id1-4.3.2.1: icao_doc1.md

#### ۴٫۳٫۲٫۱ {#id1-4.3.2.1}
The operator shall ensure that all systems are operational. [Page: 5]
<<<FILE_END:{"index": 1, "id": "id1", "name": "icao_doc1.md"}>>>
```

### مثال یک ورودی در فایل تجمیعی سورس‌کد (`text_to_markdown.py`):
```markdown
<<<FILE_START:{"index": 1, "path": "src/main.py", "size": 1234, "sha1": "..."}>>>
## 1. src/main.py

```python
def main():
    ...
```
<<<FILE_END:{"index": 1, "path": "src/main.py"}>>>
```

---

## یکتایی Anchorها بین چند سند

برای اینکه هوش مصنوعی بتواند به‌طور دقیق به یک بند خاص از یک سند خاص ارجاع دهد، هر سند یک شناسه‌ی پایه‌ی یکتا (`id1`، `id2`، ...) می‌گیرد و همه‌ی Anchorهای داخلی آن به فرم `{#id<N>-<شماره‌بند>}` یکدست می‌شوند. این کار به دو روش انجام می‌شود:

- **در یک اجرا:** وقتی چند PDF را یک‌جا با `pdf_to_markdown.py ... --combine` پردازش می‌کنید، شناسه‌ها به‌صورت پیوسته از `--id-start` (پیش‌فرض `1`) به هر سند اختصاص می‌یابند، بنابراین هیچ دو سندی Anchor یکسان نخواهند داشت.
- **بین چند اجرا (دسته‌بندی شده):** اگر اسناد را در دسته‌های مختلف پردازش می‌کنید، با `--id-start` شماره‌ی شروع دسته‌ی بعدی را برابر `LAST_ID_NUMBER + 1` دسته‌ی قبلی قرار دهید تا شناسه‌ها در کل پروژه یکتا باقی بمانند.

---

## تغییرات نسبت به نسخه‌ی قبلی

- پشتیبانی از **ورودی‌های متنوع** برای `pdf_to_markdown.py`: تک فایل، چند فایل، یک پوشه یا چند پوشه (با `-R` برای جستجوی بازگشتی).
- امکان ساخت **خروجی تجمیعی** مستقیماً از `pdf_to_markdown.py` با گزینه‌ی `--combine`، بدون نیاز به اجرای جدا‌ی `md_combiner.py`.
- یکدست‌سازی Anchorها بین چند سند و پشتیبانی از `--id-start` برای ادامه‌ی شماره‌گذاری در دسته‌های بعدی.
- بهبود تشخیص شماره‌گذاری بندها (رفع false-positive هایی مثل «2.5 months» و حفظ بندهای بدون متن بدنه).
- بهبود حذف هدر/فوتر و مهرهای رسمی تکراری، و پاک‌سازی artifact های `(cid:xx)`.
- اضافه‌شدن ابزار جدید **`text_to_markdown.py`** برای ترکیب پروژه‌های متنی/سورس‌کد در یک Markdown واحد با همان قالب جداکننده‌ها.

---

## محدودیت‌ها و نکات

- **PDF اسکن‌شده** (بدون لایه‌ی متن) نیاز به OCR (مثل Tesseract) دارد.
- **جداول پیچیده‌ی بدون خط‌کشی** ممکن است نیاز به ابزارهای تخصصی‌تر (مثل `camelot-py`) یا ویرایش دستی داشته باشند.
- **شماره‌گذاری‌های غیراستاندارد** (مثل `A.1.b`) ممکن است به‌درستی به هدینگ تبدیل نشوند و نیاز به تنظیم الگوهای تشخیص در کد داشته باشند.
- در `text_to_markdown.py`، فایل‌های بزرگ‌تر از `--max-size` به‌طور خودکار نادیده گرفته می‌شوند.

---

## عیب‌یابی

- **خطای ماژول یافت نشد:** دستور `pip install -r requirements.txt` را اجرا کنید.
- **مشکل در استخراج متن PDF:** مطمئن شوید PDF دارای لایه‌ی متن قابل‌انتخاب است (مثلاً با Adobe Reader تست کنید).
- **مشکل در جداول:** وجود خط‌کشی در جدول را بررسی کنید؛ برای جداول پیچیده از `camelot-py` استفاده کنید.
- **فایلی در خروجی `text_to_markdown.py` نیست:** بررسی کنید آیا در `DEFAULT_EXCLUDE_DIRS`/`DEFAULT_EXCLUDE_PATTERNS` یا `--exclude` حذف شده، باینری تشخیص داده شده، یا از `--max-size` بزرگ‌تر است.

---

## پشتیبانی

برای سوال یا گزارش خطا، در ریپازیتوری گیت‌هاب پروژه یک Issue باز کنید.
